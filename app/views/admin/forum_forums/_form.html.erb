<%= form_with(model: [ :admin, admin_forum_forum]) do |form| %>
  <% if admin_forum_forum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(forum_forum.errors.count, "error") %> prohibited this forum_forum from being saved:</h2>

      <ul>
        <% admin_forum_forum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
